@page
@model LegalLib.KlasifikasiIndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index Klasifikasi</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tblKlasifikasi[0].Klasifikasi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tblKlasifikasi[0].IsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tblKlasifikasi) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Klasifikasi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.KlasifikasiID">Edit</a>
            </td>
        </tr>
}
    </tbody>
</table>
